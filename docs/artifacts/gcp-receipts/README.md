# docs/artifacts/gcp-receipts/

Drop dated GCP evidence here after Cloud Run (ERAG) or FinOps apply→verify→destroy.

Suggested:

- `YYYYMMDD-cloudrun-health.txt`
- `YYYYMMDD-finops-apply-summary.txt` (redacted)
- `YYYYMMDD-destroy.txt`

No service account JSON keys in git.
